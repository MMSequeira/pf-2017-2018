class Spaceship {
  // Add variables as for the SpaceScene1 sketch.

  // Define as for the SpaceScene1 sketch.
  Spaceship(float x, float y, float angle, float scale) {
  }
  
  // Define as for the SpaceScene1 sketch.
  void setScaleTo(float newScale) {
  }
  
  // Define as for the SpaceScene1 sketch.
  void rotateTo(float newAngle) {
  }

  // Complete this method so that it changes the angle of the spaceship *by*
  // the angle variation received in parameter angleVariation. It may be a
  // good idea to ensure the angle remains between -π and π.
  void rotateBy(float angleVariation) {
  }
  
  // Define as for the SpaceScene1 sketch.
  void moveTo(float newX, float newY) {
  }
  
  // Complete this method so that it changes the position of the spaceship
  // *by* the amounts received in parameters dx and dy. The values in dx and
  // dy may be seen as components of a displacement vector.
  void moveBy(float dx, float dy) {
  }
      
  // Define as for the SpaceScene1 sketch.
  void display() {
  }
}